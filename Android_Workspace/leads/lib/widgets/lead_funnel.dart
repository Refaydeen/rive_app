import 'package:flutter/material.dart';

class LeadFunnel extends StatelessWidget {
  const LeadFunnel({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      height: 350,
      width: 250,
      color: Colors.grey,
    );
  }
}
